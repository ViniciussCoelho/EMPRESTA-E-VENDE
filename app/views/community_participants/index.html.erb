<p style="color: green"><%= notice %></p>

<h1>Community participants</h1>

<div id="community_participants">
  <% @community_participants.each do |community_participant| %>
    <%= render community_participant %>
    <p>
      <%= link_to "Show this community participant", community_participant %>
    </p>
  <% end %>
</div>

<%= link_to "New community participant", new_community_participant_path %>
