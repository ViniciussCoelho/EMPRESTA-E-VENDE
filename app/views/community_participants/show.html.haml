%p{:style => "color: green"}= notice
= render @community_participant
%div
  = link_to "Edit this community participant", edit_community_community_participant_path(@community, @community_participant)
  
  = link_to "Back to community participants", community_community_participants_path(@community)
  
  = button_to "Destroy this community participant", community_community_participant_path(@community, @community_participant), method: :delete
